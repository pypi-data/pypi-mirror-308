[build-system]
requires = ["setuptools", "wheel", "setuptools_scm"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "organelle_segmenter_plugin/_version.py"

[tool.black]
line-length = 120
