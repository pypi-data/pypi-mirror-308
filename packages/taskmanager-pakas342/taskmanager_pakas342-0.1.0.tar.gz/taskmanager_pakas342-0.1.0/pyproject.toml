[project]
name = "taskmanager_pakas342"
version = "0.1.0"
description = "Simple task manager"
requires-python = ">=3.7"
[project.scripts]
taskman = "main:main"
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["src/taskmanager_pakas342"]