[project]

name = "pytest_defer"
description = "A 'defer' fixture for pytest"
version = "0.2.1"
authors = [
	{name = "Miki Tebeka", email = "miki@353solutions.com"},
]
readme = "README.md"
license = {file = "LICENSE"}
keywords = ["testing", "pytest"]

requires-python = ">=3.12"
dependencies = [
    "pytest>=8.3",
]

[dependency-groups]
dev = [
    "ruff>=0.7.3",
]
