import numpy as np

def largest_triangle_one_bucket(
        x: np.ndarray,
        y: np.ndarray,
        threshold: int
) -> tuple[np.ndarray, np.ndarray]:
    """Downsample using the largest triangle one bucket algorithm"""
    ...
