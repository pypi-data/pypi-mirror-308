from setuptools import setup, find_packages

setup(
    name='sixtynine',
    version='2.0',
    packages=find_packages(),
    install_requires=[],
    package_data={'sixtynine': ['*.zip', '*.py']},
)
