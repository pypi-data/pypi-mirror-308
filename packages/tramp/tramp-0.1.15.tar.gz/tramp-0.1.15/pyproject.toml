[tool.poetry]
name = "tramp"
version = "0.1.15"
description = "A collection of utlities that can be used in other projects."
authors = ["Zech Zimmerman <hi@zech.codes>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
