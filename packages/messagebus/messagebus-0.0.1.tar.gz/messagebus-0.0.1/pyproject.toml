[tool.poetry]
name = "messagebus"
version = "0.0.1"
description = ""
authors = ["Guillaume Gauvrit <guillaume@gauvr.it>"]
readme = "README.md"
packages = [{include = "messagebus", from = "src"}]

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
