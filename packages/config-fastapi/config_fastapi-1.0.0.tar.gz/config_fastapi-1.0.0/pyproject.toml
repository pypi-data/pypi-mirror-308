[tool.poetry]
name = "config_fastapi"
version = "1.0.0"
description = "Configuration loader for FastAPI applications supporting multiple sources"
authors = ["Aleksandr Yurlov <sasha.yur@mail.ru>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
