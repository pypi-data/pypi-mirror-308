[tool.poetry]
name = "ingest4llm"
version = "0.0.1"
description = "Ingest different data sources into structured data for LLM"
authors = ["Hangxing Wei"]
license = "Apache-2.0 license"

[tool.poetry.dependencies]
python = "^3.10"
pillow = "^10.3.0"
httpx = "^0.27.0"
pydantic = "^2.7.4"
aiofiles = "^24.1.0"
loguru = "^0.7.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
