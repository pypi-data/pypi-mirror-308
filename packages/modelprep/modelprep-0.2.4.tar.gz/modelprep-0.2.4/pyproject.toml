[project]
name = "modelprep"
dynamic = ["version"]
description = "A CLI tool for managing and analyzing image datasets for model training"
readme = "README.md"
requires-python = ">=3.7"
license = { text = "MIT" }
authors = [
    { name = "Harsh Murari", email = "hmurari@visionify.ai" }
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "typer>=0.9.0",
    "pillow>=10.0.0",
    "rich>=13.0.0",
    "ultralytics>=8.0.0",
    "numpy>=1.22.0",
    "roboflow>=1.1.3",
    "python-dotenv>=0.19.0",
    "psutil>=5.8.0"
]

[project.urls]
Homepage = "https://github.com/visionify/modelprep"

[project.scripts]
modelprep = "modelprep.cli:app"

[build-system]
requires = ["setuptools>=45", "setuptools_scm[toml]>=6.2"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "src/modelprep/_version.py"
version_scheme = "post-release"
local_scheme = "node-and-date"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-v --tb=short"
norecursedirs = [
    "dataset",
    "dataset/*",
    "*.egg",
    "dist",
    "build",
    "__pycache__",
    "runs"
]

[project.optional-dependencies]
test = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
]
