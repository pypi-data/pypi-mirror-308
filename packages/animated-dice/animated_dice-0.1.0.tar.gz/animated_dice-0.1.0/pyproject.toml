[tool.poetry]
name = "animated-dice"
version = "0.1.0"
description = "animated dice"
authors = ["logan maguire"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12.0"
requests = "^2.32.3"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
