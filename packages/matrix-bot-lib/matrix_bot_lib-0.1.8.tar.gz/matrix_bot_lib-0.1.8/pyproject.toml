[tool.poetry]
name = "matrix-bot-lib"
#dynamic = ["version"]
version = "0.1.8"  # rememeber to also change in __init__.py
description = "Super early lib for interacting with Matrix"
readme = "README.md"
authors = ["Nicolai Søborg <git@xn--sb-lka.org>"]

[tool.poetry.dependencies]
python = "^3.10"
httpx = {extras = ["http2"], version = "^0.27.0"}
result = "^0.17.0"

[tool.poetry.dev-dependencies]
pytest = "^8.3"
mypy = "^0.971"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
