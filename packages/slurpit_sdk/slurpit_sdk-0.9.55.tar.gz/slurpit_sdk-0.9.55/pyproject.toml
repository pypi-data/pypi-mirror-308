[tool.poetry]
name = "slurpit_sdk"
version = "0.9.55"
description = "A robust Python SDK for slurpit"
readme = "README.md"
authors = ["Slurp'it <info@slurpit.io>"]
packages = [{include = "slurpit", from = "src"}]

[tool.poetry.dependencies]
python = "^3.10"
pandas = "^2.2.2"
httpx = "^0.27.0"
