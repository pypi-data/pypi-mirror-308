[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.poetry]
name = "my-datetime-project"
version = "0.1.0"
description = "A simple Python project to print current date and time"
authors = ["zhouyx <zhouyx.100@gmail.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.6"

[tool.poetry.dev-dependencies]
pytest = "^5.2"
