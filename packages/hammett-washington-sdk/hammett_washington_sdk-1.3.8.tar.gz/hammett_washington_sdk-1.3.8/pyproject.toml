[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=61.0"]
[project]
name = "hammett-washington-sdk"
description = "Vel corrupti volupt"
version = "1.3.8"
readme = "README.md"
requires-python = ">=3.7"
keywords = []
authors = [{name = "Zelenia Merrill", email = "givonys@mailinator.com"}]
urls = {Documentation = "https://testing.com"}
dependencies = ["apimatic-core~=0.2.0, >= 0.2.15", "apimatic-core-interfaces~=0.1.0, >= 0.1.5", "apimatic-requests-client-adapter~=0.1.0, >= 0.1.6"]
classifiers = []
[project.optional-dependencies]
testutils = ["pytest>=7.2.2"]
[tool.setuptools.packages.find]
exclude = ["tests", "tests.*"]
